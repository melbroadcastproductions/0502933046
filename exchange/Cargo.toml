[package]
name = "flowsurface-exchange"
description = "Adapters for data providers/crypto exchanges to work with Flowsurface"
version.workspace = true
license = "MIT"
edition.workspace = true
repository.workspace = true
homepage.workspace = true

[features]
default = []
# Use an unbounded mpsc channel for event delivery instead of the default
# bounded-512.  When enabled, events are never dropped under load — the
# channel grows to whatever heap the device has.  Useful for database
# ingestion or audit trails where every tick matters.
unbounded-channel = []

[dependencies]
chrono.workspace = true
serde.workspace = true
serde_json.workspace = true
log.workspace = true
thiserror.workspace = true
enum-map.workspace = true
rustc-hash.workspace = true
url.workspace = true
rand.workspace = true

tokio = { version = "1.52", default-features = false, features = ["rt", "macros"] }
reqwest = { version = "0.12.28", default-features = false, features = ["json", "brotli", "rustls-tls", "socks"] }
bytes = "1.11.1"
sonic-rs = { version = "0.5.8", default-features = false }
fastwebsockets = { version = "0.10.0", default-features = false, features = ["upgrade"] }
http-body-util = "0.1.3"
hyper = { version = "1", default-features = false, features = ["http1", "client"] }
hyper-util = { version = "0.1.20", default-features = false }
tokio-rustls = "0.24.1"
webpki-roots = "0.23.1"
zip = { version = "8.6.0", default-features = false, features = ["deflate-flate2-zlib-rs"] }
csv = "1.4.0"
tokio-socks = "0.5.2"
base64 = "0.22.1"
futures = { version = "0.3.32", default-features = false, features = ["std", "async-await"] }
